// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/eslsoft/vocnet/internal/infrastructure/database/ent/predicate"
	"github.com/eslsoft/vocnet/internal/infrastructure/database/ent/userword"
)

// UserWordQuery is the builder for querying UserWord entities.
type UserWordQuery struct {
	config
	ctx        *QueryContext
	order      []userword.OrderOption
	inters     []Interceptor
	predicates []predicate.UserWord
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserWordQuery builder.
func (uwq *UserWordQuery) Where(ps ...predicate.UserWord) *UserWordQuery {
	uwq.predicates = append(uwq.predicates, ps...)
	return uwq
}

// Limit the number of records to be returned by this query.
func (uwq *UserWordQuery) Limit(limit int) *UserWordQuery {
	uwq.ctx.Limit = &limit
	return uwq
}

// Offset to start from.
func (uwq *UserWordQuery) Offset(offset int) *UserWordQuery {
	uwq.ctx.Offset = &offset
	return uwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uwq *UserWordQuery) Unique(unique bool) *UserWordQuery {
	uwq.ctx.Unique = &unique
	return uwq
}

// Order specifies how the records should be ordered.
func (uwq *UserWordQuery) Order(o ...userword.OrderOption) *UserWordQuery {
	uwq.order = append(uwq.order, o...)
	return uwq
}

// First returns the first UserWord entity from the query.
// Returns a *NotFoundError when no UserWord was found.
func (uwq *UserWordQuery) First(ctx context.Context) (*UserWord, error) {
	nodes, err := uwq.Limit(1).All(setContextOp(ctx, uwq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userword.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uwq *UserWordQuery) FirstX(ctx context.Context) *UserWord {
	node, err := uwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserWord ID from the query.
// Returns a *NotFoundError when no UserWord ID was found.
func (uwq *UserWordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uwq.Limit(1).IDs(setContextOp(ctx, uwq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userword.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uwq *UserWordQuery) FirstIDX(ctx context.Context) int {
	id, err := uwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserWord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserWord entity is found.
// Returns a *NotFoundError when no UserWord entities are found.
func (uwq *UserWordQuery) Only(ctx context.Context) (*UserWord, error) {
	nodes, err := uwq.Limit(2).All(setContextOp(ctx, uwq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userword.Label}
	default:
		return nil, &NotSingularError{userword.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uwq *UserWordQuery) OnlyX(ctx context.Context) *UserWord {
	node, err := uwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserWord ID in the query.
// Returns a *NotSingularError when more than one UserWord ID is found.
// Returns a *NotFoundError when no entities are found.
func (uwq *UserWordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uwq.Limit(2).IDs(setContextOp(ctx, uwq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userword.Label}
	default:
		err = &NotSingularError{userword.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uwq *UserWordQuery) OnlyIDX(ctx context.Context) int {
	id, err := uwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserWords.
func (uwq *UserWordQuery) All(ctx context.Context) ([]*UserWord, error) {
	ctx = setContextOp(ctx, uwq.ctx, ent.OpQueryAll)
	if err := uwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserWord, *UserWordQuery]()
	return withInterceptors[[]*UserWord](ctx, uwq, qr, uwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uwq *UserWordQuery) AllX(ctx context.Context) []*UserWord {
	nodes, err := uwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserWord IDs.
func (uwq *UserWordQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uwq.ctx.Unique == nil && uwq.path != nil {
		uwq.Unique(true)
	}
	ctx = setContextOp(ctx, uwq.ctx, ent.OpQueryIDs)
	if err = uwq.Select(userword.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uwq *UserWordQuery) IDsX(ctx context.Context) []int {
	ids, err := uwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uwq *UserWordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uwq.ctx, ent.OpQueryCount)
	if err := uwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uwq, querierCount[*UserWordQuery](), uwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uwq *UserWordQuery) CountX(ctx context.Context) int {
	count, err := uwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uwq *UserWordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uwq.ctx, ent.OpQueryExist)
	switch _, err := uwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uwq *UserWordQuery) ExistX(ctx context.Context) bool {
	exist, err := uwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserWordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uwq *UserWordQuery) Clone() *UserWordQuery {
	if uwq == nil {
		return nil
	}
	return &UserWordQuery{
		config:     uwq.config,
		ctx:        uwq.ctx.Clone(),
		order:      append([]userword.OrderOption{}, uwq.order...),
		inters:     append([]Interceptor{}, uwq.inters...),
		predicates: append([]predicate.UserWord{}, uwq.predicates...),
		// clone intermediate query.
		sql:  uwq.sql.Clone(),
		path: uwq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserWord.Query().
//		GroupBy(userword.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uwq *UserWordQuery) GroupBy(field string, fields ...string) *UserWordGroupBy {
	uwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserWordGroupBy{build: uwq}
	grbuild.flds = &uwq.ctx.Fields
	grbuild.label = userword.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//	}
//
//	client.UserWord.Query().
//		Select(userword.FieldUserID).
//		Scan(ctx, &v)
func (uwq *UserWordQuery) Select(fields ...string) *UserWordSelect {
	uwq.ctx.Fields = append(uwq.ctx.Fields, fields...)
	sbuild := &UserWordSelect{UserWordQuery: uwq}
	sbuild.label = userword.Label
	sbuild.flds, sbuild.scan = &uwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserWordSelect configured with the given aggregations.
func (uwq *UserWordQuery) Aggregate(fns ...AggregateFunc) *UserWordSelect {
	return uwq.Select().Aggregate(fns...)
}

func (uwq *UserWordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uwq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uwq); err != nil {
				return err
			}
		}
	}
	for _, f := range uwq.ctx.Fields {
		if !userword.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uwq.path != nil {
		prev, err := uwq.path(ctx)
		if err != nil {
			return err
		}
		uwq.sql = prev
	}
	return nil
}

func (uwq *UserWordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserWord, error) {
	var (
		nodes = []*UserWord{}
		_spec = uwq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserWord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserWord{config: uwq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uwq *UserWordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uwq.querySpec()
	_spec.Node.Columns = uwq.ctx.Fields
	if len(uwq.ctx.Fields) > 0 {
		_spec.Unique = uwq.ctx.Unique != nil && *uwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uwq.driver, _spec)
}

func (uwq *UserWordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userword.Table, userword.Columns, sqlgraph.NewFieldSpec(userword.FieldID, field.TypeInt))
	_spec.From = uwq.sql
	if unique := uwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uwq.path != nil {
		_spec.Unique = true
	}
	if fields := uwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userword.FieldID)
		for i := range fields {
			if fields[i] != userword.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uwq *UserWordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uwq.driver.Dialect())
	t1 := builder.Table(userword.Table)
	columns := uwq.ctx.Fields
	if len(columns) == 0 {
		columns = userword.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uwq.sql != nil {
		selector = uwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uwq.ctx.Unique != nil && *uwq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uwq.predicates {
		p(selector)
	}
	for _, p := range uwq.order {
		p(selector)
	}
	if offset := uwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserWordGroupBy is the group-by builder for UserWord entities.
type UserWordGroupBy struct {
	selector
	build *UserWordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uwgb *UserWordGroupBy) Aggregate(fns ...AggregateFunc) *UserWordGroupBy {
	uwgb.fns = append(uwgb.fns, fns...)
	return uwgb
}

// Scan applies the selector query and scans the result into the given value.
func (uwgb *UserWordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uwgb.build.ctx, ent.OpQueryGroupBy)
	if err := uwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserWordQuery, *UserWordGroupBy](ctx, uwgb.build, uwgb, uwgb.build.inters, v)
}

func (uwgb *UserWordGroupBy) sqlScan(ctx context.Context, root *UserWordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uwgb.fns))
	for _, fn := range uwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uwgb.flds)+len(uwgb.fns))
		for _, f := range *uwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserWordSelect is the builder for selecting fields of UserWord entities.
type UserWordSelect struct {
	*UserWordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uws *UserWordSelect) Aggregate(fns ...AggregateFunc) *UserWordSelect {
	uws.fns = append(uws.fns, fns...)
	return uws
}

// Scan applies the selector query and scans the result into the given value.
func (uws *UserWordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uws.ctx, ent.OpQuerySelect)
	if err := uws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserWordQuery, *UserWordSelect](ctx, uws.UserWordQuery, uws, uws.inters, v)
}

func (uws *UserWordSelect) sqlScan(ctx context.Context, root *UserWordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uws.fns))
	for _, fn := range uws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
